# Build scratch / extracted trees
build_work/
**/build_work/
vasp.*/

# Licensed VASP testsuite (never commit). Overlays live in testsuite_overlays/.
/testsuite/

# Portable package outputs (ZIP + unpacked dirs)
*.zip
*.sha256
vasp-*-msys2-portable/

# Source / archive inputs (licensed — never commit)
*.tgz
*.tar.gz
*.tar.bz2
*.tar.xz
vasp*.zip

# Pseudopotentials (licensed)
POTCAR
potpaw*
**/POTCAR
**/potpaw*

# Generated binaries / package contents
*.exe
*.dll
*.lib
*.a
*.o
*.obj
*.mod
*.pdb

# Editor / OS junk
.DS_Store
Thumbs.db
Desktop.ini
*~
*.swp
*.swo
.vscode/
.idea/
# Cursor local state (never commit)
.cursor/

# Local env / secrets
.env
.env.*
toolchain/local.env
*.pem
*.key
credentials*
secrets*

# Local build logs
*.log
full_build_wrap.log

# Optional-library toolchain prefixes and build trees (never commit)
toolchain/install/
toolchain/build/


# Local smoke scratch (may contain POTCAR)
smoke_wave2/

